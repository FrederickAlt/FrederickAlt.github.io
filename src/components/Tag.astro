---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-3">
  {tags.map(tag => (
    <span class="px-4 py-2 bg-background border border-primary-500/50 text-primary-400 rounded-lg text-sm font-semibold">
      {tag}
    </span>
  ))}
</div>
